name: mesclainvest
description: Plataforma de investimentos simulados em startups universitárias - PUC-Campinas

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^2.32.0
  firebase_auth: ^4.19.6
  cloud_firestore: ^4.17.5

  cupertino_icons: ^1.0.6
  google_fonts: ^6.1.0
  mask_text_input_formatter: ^2.9.0
  url_launcher: ^6.2.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true